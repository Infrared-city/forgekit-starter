export type LngLatType = { lng: number; lat: number }

// Re-export plugin types from shared for consumer convenience
export type {
  KeyboardShortcut,
  MapEvent,
  MapOverlayProps,
  MapPanelProps,
  MapPlugin,
  MapPluginContext,
  MapViewport,
} from '@forge-kit/plugin-contracts'
